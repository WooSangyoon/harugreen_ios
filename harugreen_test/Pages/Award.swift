import SwiftUI

struct Award: View {
    
    
    var body: some View {
        ZStack{
            VStack{
                HStack{
                    Text("도전과제")
                        .padding(30)
                        .font(.system(size: 30, weight: .bold))
                    Spacer()
                }
                .padding(.bottom, -30)
                
              
                Spacer()
                 
                
                
                
            }
        }
    }
}
